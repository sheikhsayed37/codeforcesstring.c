#include <stdio.h>
int main()
{
    char st[1001],sd[1222];
    scanf("%s %s",&st,&sd);
     int value = strcmp(st,sd);
    if(st>0)

    
    {

     printf("%s",st);

    }
    else if(st<0)

    {
        printf("%s",st);
    }
}