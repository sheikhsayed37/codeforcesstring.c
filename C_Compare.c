#include <stdio.h>
#include <string.h>
int main()
{
    char st[100000],sd[100000];
    scanf("%s %s",&st,&sd);
     int value = strcmp(st,sd);
    if(value<=0)

    
    {

     printf("%s\n",st);

    }
    else 

    {
        printf("%s\n",st);
    }
}
